upstream client {
  server locahost:3000;
}

upstream server {
  server locahost:8000;
}

server {
    listen 4000;

    location / {
        proxy_pass http://client;
    }

    location /api {
        proxy_pass http://server;
    }
}
